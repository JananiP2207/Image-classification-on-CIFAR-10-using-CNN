**CIFAR-10 Image Classification using CNN**

This project builds an image classification model using a Convolutional Neural Network (CNN) on the CIFAR-10 dataset. The model learns to classify images into 10 different categories using deep learning techniques.

**Dataset:**
**CIFAR-10**
60,000 color images (32×32)
**10 classes:**
Airplane, Automobile, Bird, Cat, Deer, Dog, Frog, Horse, Ship, Truck

**Model:**

Convolutional layers with ReLU
MaxPooling layers
Fully connected (Dense) layers
Softmax output for multi-class classification

**Tools & Libraries:**

Python
TensorFlow / Keras
NumPy
Matplotlib
Jupyter Notebook

**Steps:**

Load and preprocess CIFAR-10 dataset
Normalize images
Build CNN model
Train the model
Evaluate performance
Make predictions
